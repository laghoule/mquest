# TODO - Codebase Improvements & Refactoring Priorities

_Generated by AI Analysis of src/ directory. Review and validate all suggestions._

## Core System Issues (High Priority)

- **Animation & Positioning**: The movement system relies heavily on `pending_tick` for position adjustments (`action.asm`:52, `input.asm`:67). This creates instability and potential timing issues. Needs a more robust tick-based animation system with proper interpolation or delta-time calculations.
- **Collision Detection Gaps**:
  - Character-to-character collision exists in `collis.asm` but is inconsistently applied (see below)
  - Scene transitions (`transit.asm`) only check object collisions, missing character interaction checks during boundary crossings
- Use JA/JB (not-signed) instead of JG/JL (signed).
- Ensure memory aligment for 8086 (16 bit)

## Code Quality & Refactoring Needed

- **AABB Collision Duplication** (`collis.asm`):
  - Lines ~134-148 and ~214-223 contain duplicate hitbox setup logic ("copy/paste" issues)
  - Hardcoded character limit at line 173: `MOV CX, 2` needs dynamic calculation for scalability
- **Variable Naming Consistency**: Systematic review required across all files to rename opaque memory-offset identifiers. Focus on establishing clear `_addr`, `_ptr`, and other naming conventions per TODO item #3 & #6 in original list.

## Documentation Improvements

- **Procedural → Algorithmic Comments**: Most comments describe "what" the code does rather than "why". Need algorithm-level explanations, especially around collision choices (AABB vs circle), tick-based movement decisions, and hardcoded limits.
